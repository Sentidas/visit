FROM eclipse-temurin:21-jdk

WORKDIR /app
COPY . .
WORKDIR /app/country

RUN ../gradlew :country:build -x test
ENTRYPOINT ["java", "-jar", "build/libs/country-0.0.1-SNAPSHOT.jar", "--spring.profiles.active=docker"]